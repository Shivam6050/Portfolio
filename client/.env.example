# Backend origin for a separately hosted API (no trailing /api).
# Leave blank when the frontend host proxies /api to the backend.
# Public build-time backend origin only (no /api suffix). Rebuild after changing; never store secrets in VITE_ variables.
VITE_API_URL=
