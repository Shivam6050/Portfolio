/**
 * Portrait URL points to a separately cached asset. Replace public/assets/profile.jpg or this URL; public URLs omit the /public directory. Do not embed the bitmap into JavaScript again.
 * Editing map: CODE_GUIDE.md at repository root.
 */
/* Public portrait asset URL; replace the separate file to update the picture without embedding it in JavaScript. */
export const PROFILE_PHOTO = "/assets/profile.jpg";
